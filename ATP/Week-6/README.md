# Week 6

This folder continues with more advanced React patterns and mini-projects.

## File Information

- **`ContextApiSync` (directory)**: Examples and code demonstrating the use of React's Context API for global state management.
- **`Create_Table_Dinamically` (directory)**: React components for generating and rendering tables dynamically from data arrays.
- **`Miniproject` (directory)**: A consolidated small React project bringing together various concepts learned so far.
- **`React-Frontend` (directory)**: A more structured, typical frontend application setup using React.
